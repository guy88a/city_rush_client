using UnityEngine;

namespace CityRush.Units
{
    [DisallowMultipleComponent]
    public sealed class Lockpickable : MonoBehaviour
    {
        // Placeholder. Later this will open lockpick UI / minigame.
        public bool TryInteract()
        {
            return false;
        }
    }
}
