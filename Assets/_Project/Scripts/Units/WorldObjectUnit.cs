using UnityEngine;

namespace CityRush.Units
{
    // Concrete so you can attach it to scene/prefab objects (GameUnit is abstract).
    public class WorldObjectUnit : GameUnit
    {
        // Intentionally empty for now.
        // Next we’ll add mechanics via components (Interactable, Pickup, Health, etc.).
    }
}
